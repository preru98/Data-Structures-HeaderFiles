#include "linkedlistVer3.h"
using namespace std;

int main(){
    int array[]={23,77,21,90, 56, 345};
    int size=sizeof(array)/sizeof(array[0]);
    insert_at_end_array(array,size);
    display(start);
}